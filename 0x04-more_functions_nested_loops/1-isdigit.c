#include "main.h"
/**
 * _isdigit - function checks for digit 0 through9
 * @c: interger to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
